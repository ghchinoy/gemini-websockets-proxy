# Gemini Multimodal Live API WebSockets proxy - nodejs


## Prerequisites

Copy `.env.environment` to `.env` and uncomment either `GEMINI_API_KEY` or `GOOGLE_CLOUD_PROJECT` and add your credentials.


## To start

```
npm run start:dev
```